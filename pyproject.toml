[project]
name = "research-notebook"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "black>=26.5.1",
    "ruff>=0.16.8",
    "zensical>=0.0.64",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
    "ruff>=0.16.8",
    "zensical>=0.0.64",
]
